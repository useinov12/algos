import React from 'react'

function PlayMenu({syncMode}) {


    const render = (syncMode) =>{
        return syncMode ? () : ()


    }

    return (
        <div>
            
        </div>
    )
}

export default PlayMenu
